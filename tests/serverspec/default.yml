- hosts: localhost
  roles:
    - ansible-role-firstboot-freebsd-update
  vars:
    # XXX you usually do not need any flags in this variable. just for testing
    # purpose.
    firstboot_freebsd_update_flags: --not-running-from-cron
